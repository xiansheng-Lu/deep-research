# M2-6 信源元数据抽取 · 前端对接交接单

> 提出方：后端开发
> 日期：2026-09-14
> 接收方：前端开发（WP-13/WP-14 证据流与 SourceBadge 回归）
> 关联里程碑：M2-6（信源元数据抽取），验收见阶段方案 §9 AC-1~AC-12
> 机读契约：**无变化**，继续冻结在 `docs/contract/openapi-m2-5.json`（24 路径）；本包不产 openapi-m2-6
> 阶段方案：`docs/design/AI研究者助手-M2-6信源元数据抽取技术方案.md`（v1.0 已冻结，含实现冻结记录 §13）
> 状态：**待前端真链回归**——本包对前端是**字段值质量修复，字段集零变化**，无新对接点；回归后按 §5 关闭归档

---

## 0. 本次对接结论（TL;DR）

1. **零新增 REST/WS 端点、零 schema 字段变化、零数据库迁移**；前端 WP-8 已写好的 `EvidenceResponse` 五值枚举、`evidence.fetched` 帧、SourceBadge/EvidenceCard 直接消费真实值即可，**无需改类型、无需加接口调用**。
2. 修复的是 M1/M2-4 遗留的「字段在、值不准」：真链证据此前 `relevance_score` 恒 0、`source_type` 几乎全是假 news、权威域名可信度被误降到 C、发布时间无页面级回退。
3. 前端唯一动作：按 §4 做证据字段值的**真链分布抽验**与 SourceBadge 色编码回归（无新功能联调）。

## 1. 联调环境

与 M2-5 相同：后端 http://localhost:8000（docker-compose.min.yml PG，alembic head 仍为 **0004，本包无迁移**）、前端 `npm run dev` + `VITE_METADATA` off、账号 dev@example.com。

新增配置（`.env.example` 已同步，默认即可启动）：

| 变量 | 默认 | 前端是否关心 |
| --- | --- | --- |
| `SOURCE_PAGE_METADATA_ENABLED` | true | 否；关闭后发布时间只取 Provider |
| `SOURCE_PAGE_FETCH_TIMEOUT_SECONDS` | 5 | 否 |
| `SOURCE_PAGE_FETCH_PER_SUBQUESTION` | 3 | 否；影响发布时间命中率，不影响字段 |

## 2. 值语义变化（字段名不变，值变真实）

| 字段 | 修复前（真链） | 修复后（M2-6） |
| --- | --- | --- |
| `source_type` | 博查 WEB 命中几乎全标 `news`（政府公告/论坛帖也是 news） | 按域名规则真实产出 `official_doc / news / community / search`（`internal` 本包不产出，属 M5） |
| `source_level` | 仅 6 个英文后缀，中国权威源大量漏判为 tertiary | 扩充政府/国际组织/学术/主流媒体/社区规则表，primary/secondary/tertiary 真实分布 |
| `credibility` | relevance 真链恒 0 → 全部降两档，**政府站也评 C** | 权威定基准、相关性至多降一档、**primary 地板 B**：stats.gov.cn 等高相关权威源为 A |
| `relevance_score` | 恒 0.0（fan-out 不写、博查无分） | 每条按子问题词面相关性打分（0~1，三位小数），与 Tavily provider 分 0.5/0.5 融合 |
| `published_at` | Provider 不返即永久 null | Provider 优先；缺失时小预算抓页面 HTML 元数据补采（默认每子问题≤3 条、5s、失败静默） |

规则判定优先级（供解释 SourceBadge 异常归类）：PRIMARY_OFFICIAL（gov.cn/gov/who.int 等）→ PRIMARY_ACADEMIC（edu/ac/arxiv/nature/ieee 等）→ SECONDARY_NEWS（新华网/人民网/央视/财新/路透/BBC 等）→ TERTIARY_COMMUNITY（知乎/微博/CSDN/Stack Overflow/github 等）→ 独立 `news` 标签弱兜底 → 默认 tertiary/search。`fake-news.example.com` 这类连写/拼接域名**不会**误判为媒体。

## 3. 契约不变项（逐字段确认）

- `GET /runs/{id}/evidence`、`GET /runs/{id}/evidence/{evidence_id}` 响应字段、分页、`include_excluded` 参数均不变；WS `evidence.fetched` 七项载荷不变。
- 枚举值域不变：`source_type=official_doc|news|community|search|internal`、`source_level=primary|secondary|tertiary`、`credibility=A|B|C|D`——前端 WP-8 的 i18n/色编码原样适用。
- 后端新增的内部留痕字段 `Evidence.metadata_`（source_rule/relevance/published_at_source/site_name/missing_fields）**不进任何 REST/WS 响应**；PRD「来源信息不全」UI 标记随 M2-7/M3 再冻结对外字段，本包前端不处理。
- `relevance_score` 仍在响应里（M2-4 已暴露），前端当前不展示该值，无需新 UI。

## 4. 前端回归清单（字段值抽验，无新功能）

- [ ] 发起一次真实研究（quick/standard），看板证据流中 `source_type` 出现真实分布：政府/统计/学术类显示「官方文档」、主流媒体「新闻」、知乎/CSDN/Stack Overflow「社区」、普通站点「搜索」，不再几乎全是「新闻」。
- [ ] SourceBadge：权威官方源可见 A 档色编码（修复前同样的 stats.gov.cn 证据显示 C）；主流媒体高相关 B、低相关 C；社区 C/D。
- [ ] 时间分布：部分 Provider 无日期的证据在检索中（默认开启补采）能补到 `published_at`；补采不到的仍为 null，不报错、不阻塞证据卡片。
- [ ] 排序：证据流/列表仍可用（后端子问题内按相关性、跨节点按可信度→相关性排序；无前端契约变化）。
- [ ] 回归：M2-4 证据分页/`include_excluded`、M2-5 exclude_evidence 剔除、EvidenceCard hover/懒加载全文均不回归；`npm run typecheck/lint/build` 全绿。
- [ ] 无需为 `internal` 做任何处理（本包后端不产出该值）。

## 5. 关闭与归档闭环

1. 前端按 §4 完成真链抽验后回填本单回归结论（实测 run 的字段分布样例 + 门禁结果）；
2. 双方无异议后文首状态改为「已回归关闭」，移入 `docs/feedback/archive/`；
3. 若发现域名归类与常识不符（误判/漏判权威源），在本单追加具体域名样例反馈后端——规则表带单测、可快速迭代，不阻塞本单其他项关闭；
4. 不在本包的诉求（证据簇/转载去重、internal 私域标记、「来源信息不全」UI、M2-7 报告 blocks 与 citations）以阶段方案 §1.1 为准，流转 M2-7/M3/M5。

## 6. 后端可复现证据

- 新增：`app/retrieval/source_rules.py`（规则分类器）、`ranker.py`（打分/融合/排序）、`page_metadata.py`（页面补采）；改造 `researcher_fan_out.py`/`standardizer.py`/`state.py`/`persistence.py`/`config.py`。
- 单测：`test_source_rules.py`（AC-1/2 规则与仿冒修正）、`test_ranker.py`（AC-3/4 打分/融合/截断）、`test_page_metadata.py`（AC-7 开关/配额/静默降级，httpx MockTransport 离线）；更新 `test_standardizer.py`（AC-5 新矩阵）、`test_researcher_fan_out.py`（AC-6/8 占位中性化/去重不回归）。
- 真库：`test_integration_m26.py`（AC-9/10，m26_orm schema：stats.gov.cn→official_doc/primary/A、reuters 低相关→news/secondary/C、zhihu→community/tertiary/C，metadata_ JSONB 往返、missing_fields、无迁移 head=0004）。
- 全量门禁：**576 passed**（含全部真库集成，无跳过）；本批 ruff 全净；`mypy app` 54 条相对基线 57 条零新增（-3 为旧错误随重写消除）。
- 契约零变化（AC-11）：以当前 `app.openapi()` 重新导出与 `docs/contract/openapi-m2-5.json` 内容空 diff 自证（文件已还原，未产生新快照）。
