# M2-5 用户介入接口 · 前端对接交接单

> 提出方：后端开发
> 日期：2026-09-14
> 接收方：前端开发（WP-15）
> 关联里程碑：M2-5（用户介入：pause/resume/cancel/intervene），验收见阶段方案 §10 AC-1~AC-17
> 后端分支：`feature/backend-m2-5-intervention`（dev squash 哈希：合入后回填）
> 机读契约：`docs/contract/openapi-m2-5.json`（24 路径累积冻结 = M2-4 二十路径 + 本批四路径，字段以该文件为唯一事实源）
> 阶段方案：`docs/design/AI研究者助手-M2-5用户介入接口技术方案.md`（v1.0 已冻结，含实现冻结记录）
> 状态：**联调中，缺陷已修复，待前端复测**——前端真链回归（2026-09-14）发现 2 项后端问题；后端已于当日修复：§9.1 resume 全分支阻塞（P0，已修复并补组合链真库用例）、§9.2 ask_followup 受理窗口（P1，已裁定并随阶段实时性修复一并解决）。请前端按 §9.3 复测 AC-2/AC-5/AC-6/AC-8，全过后双方按 §8 关闭归档

---

## 0. 本次对接结论（TL;DR）

1. WP-15 依赖的四个 REST 控制点全部就绪，此前 mock 模拟的 pause/resume/cancel/intervene 真链 404 现象消失：`POST /api/v1/runs/{id}/pause`、`/resume`、`/cancel`、`/intervene`。
2. WS 通道新增 `intervene` / `cancel` 客户端指令，与 REST **复用同一 service**，回 `intervene.ack` / `intervene.error`，`request_id` 原样透传；pause/resume **无 WS 指令**，只走 REST（与前端 `ClientCommandType` 声明一致）。
3. WS 握手新增 **run 创建者归属校验**：合法 token 但订阅他人 run 直接 1008 拒绝（前端原有「连接建立时已做归属校验」的假设现在才真正成立）。
4. 澄清挂起真链打通：挂起时在 `run.finished(paused)` 之前推 `interrupt.requested`（questions/defaults/expires_in_seconds）；刷新页面后 `GET /runs/{id}` 的新增可选字段 `interrupt` 可恢复澄清卡，服务端不做帧回放。
5. 前端 `types.ts` / `realtime/types.ts` 预置的请求体、响应、ACK 信封与冻结契约**逐字段核对差异为 0**（本单 §3.1）；`revert_stage` / `mark_doubt` 前端联合类型里保留但 M2 不开放，后端对其 422。
6. 联调必须 `VITE_MOCK=off`；启动前需 `alembic upgrade head`（本次新增 0004 迁移，建 `run_interventions` 表）。

## 1. 联调环境

与《M2-3实时成本与M2-4看板接口交接》§1 相同：后端 http://localhost:8000（`docker-compose.min.yml` 起 PG → **`uv run alembic upgrade head`（本次必须，0004）** → `uv run uvicorn app.main:app --reload`），前端 `npm run dev` 且 `VITE_MOCK=off`，账号 dev@example.com / Dev@123456，Bearer 鉴权。

新增配置项（`.env.example` 已同步，默认值可直接启动）：

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `CLARIFICATION_EXPIRES_SECONDS` | 900 | `interrupt.requested` 帧与 `RunResponse.interrupt` 的展示超时秒数；**仅展示语义**，后端不做定时超时、不自动注入默认答案（前端按既有口径只读标注「已按默认假设」） |

通用口径不变：无 token → 401；资源不存在或非 run 创建者 → 一律 404（不泄漏存在性）。

## 2. REST 契约（已冻结，逐字段见 openapi-m2-5.json）

### 2.1 本批新增端点一览

| 方法与路径 | 请求体 | 200 响应 | 主要拒绝 |
| --- | --- | --- | --- |
| POST `/api/v1/runs/{run_id}/pause` | `{reason?: string(≤256)}` | `RunControlResponse{run_id,status:"paused"}` | 409 `RUN_NOT_PAUSABLE`（非 running/在途无协程的孤儿 running）、409 `RUN_ALREADY_PAUSED` |
| POST `/api/v1/runs/{run_id}/resume` | `{human_input?: HumanInput}` | `RunControlResponse{run_id,status:"running"}` | 409 `RUN_NOT_RESUMABLE`；422 三分支互斥/澄清缺答案 |
| POST `/api/v1/runs/{run_id}/cancel` | `{reason?: string, keep_partial?: bool=true}` | `RunControlResponse{run_id,status,partial_report_id?}` | 终态幂等返回实际 status，不改状态、不重复发帧 |
| POST `/api/v1/runs/{run_id}/intervene` | `InterventionAction{type, payload}` | `RunControlResponse{run_id,status}` | 409 `INTERVENE_NOT_ALLOWED`；422 载荷非法；404 引用资源跨 run/不存在 |

业务判别码位置与 M2-2 裁决 409 同一形态：HTTP 信封 `{code:"conflict", message, details:{code:"RUN_NOT_PAUSABLE", ...}}`，前端按 `details.code` / message 映射（mock 预置的映射不变）。

### 2.2 状态机与允许矩阵（重点）

```text
pending ──首跑──▶ running ──pause──▶ paused ──resume──▶ running
                    │  ▲              │  ▲
        澄清/裁决/成本挂起(已有路径)   │  └────cancel────▶ cancelled
                    │                │
                    └────cancel───────┘
running/paused ──cancel──▶ cancelled（终态，不可恢复）
```

| 端点 \ 当前状态 | pending | running | paused | succeeded/failed | cancelled |
| --- | --- | --- | --- | --- | --- |
| pause | 409 RUN_NOT_PAUSABLE | 200 | 409 RUN_ALREADY_PAUSED | 409 | 409 |
| resume | 409 | 409 RUN_NOT_RESUMABLE | 200 | 409 | 409 |
| cancel | 200 | 200 | 200 | 200 幂等 | 200 幂等 |
| intervene ask_followup | 409 | **仅 current_stage=retrieve** | 409 | 409 | 409 |
| intervene exclude_evidence | 409 | current_stage ∈ retrieve/standardize/critique | 409 | 409 | 409 |

### 2.3 时序收敛口径（pause/cancel 必读）

- pause/cancel 的 REST 200 **先返回**，执行协程在下一 await 点（super-step 边界）才落库收尾；极端竞态下 `GET` 可能瞬时读到旧状态。最终状态以随后到达的 `run.finished(status=paused/cancelled)` 帧 + GET 详情收敛——与 M2-2 起「帧为最终收敛」的既有模式一致，前端不要只凭 200 响应渲染终态。
- pause 后当前阶段行**保持 running**（不新增 stage cancelled/paused 枚举），run 终态是唯一事实；`finished_at` 不置位。
- cancel 置 `cancelled` + `finished_at`；`keep_partial=true` 且存在报告草稿时 200 带回 `partial_report_id`（M2 报告为一次性确定性拼装，多数取消点无草稿，字段缺省是正常态）。
- running 行但进程内无在途协程（API 重启孤儿）时 pause 返回 409，不假成功；孤儿清扫在 M2-8。

### 2.4 resume：HumanInput 三分支

```jsonc
// 三选一互斥（同时出现多个键 422）；空 body 等价 {"human_input":{"kind":"proceed"}}
{
  "answers": { "scope": "近三年", "verdicts": {"conf_...": {"choice":"evidence_a","reason":"..."}} },
  "action":  { "type": "ask_followup", "payload": {"sub_question_id":"...", "question":"..."} },
  "kind": "proceed"
}
```

| 挂起原因 | 允许输入 | 行为 |
| --- | --- | --- |
| clarify | 仅 `answers`（≥1 个非空答案键，verdicts 不计入） | 合并为 state.clarification 回流，**不二次挂起**；空 answers/proceed → 422 |
| critique（high 冲突挂起） | `answers.verdicts` 或 proceed | 裁决主通道仍是 `/conflicts/{id}/verdict`，本分支仅统一入口完整性 |
| cost（user_intervention） | `kind=proceed` 或空 body | 空节点 → report，即 M2-4 成本闸门实测恢复路径 |
| 手动软暂停 | `kind=proceed` 或空 body | 从最近 checkpoint 续跑当前阶段 |

`action` 分支经统一 resume 入口返回 409 `INTERVENE_NOT_ALLOWED`（running/paused 介入统一走 `/intervene`，paused 不支持补查）。

### 2.5 intervene：动作语义

- `ask_followup`：payload `{sub_question_id(必填,本 run 子问题), question(1~1000 字非空), reason?}`；run 保持 running，动作入 `run_interventions` 队列（pending），fan-out **下一层边界**消费：新建子问题（depends_on 指向原子问题）并发 `sub_question.created/started/finished`，追加证据进同一份报告。
- `exclude_evidence`：payload `{evidence_id(必填,本 run 证据), reason?}`；**同步**置 `excluded_by_user=true` 并提交（看板 REST 立即生效），介入行直接记 applied；剔除发生在 retrieve 时证据不进 standardize；standardize/critique 时 critic 冲突输入与 reporter 论断均不再含该证据；**已落库冲突/已生成报告不追溯**。
- 幂等：REST 可选 `Idempotency-Key` 请求头（≤64 字符），双击只入队一次、返回同一 200；**WS 指令无此头，不启用服务端幂等**（前端按钮防抖兜底）。
- 404：`sub_question_id` / `evidence_id` 不存在或跨 run（与归属同口径，不区分「不存在」与「他人的」）。

### 2.6 RunResponse 增量（超集，可选字段）

```jsonc
"interrupt": {
  "reason": "clarify",
  "questions": [{"key":"scope","text":"...","options":[],"recommended":null}],
  "defaults": {},
  "expires_in_seconds": 900
} // 仅 paused@clarify 非空；其余状态/非澄清挂起恒为 null
```

## 3. WS 控制指令与实时帧（不入 OpenAPI）

频道与信封不变：频道 `runs:{run_id}`，推送信封 `{v,event_id,ts,run_id,stage,type,payload}`。

### 3.1 客户端指令（连接内，复用 REST 同一 service）

发送（前端 `sendCommand(type, payload)` 既有形态）：

```jsonc
{"v":"1.0","type":"intervene","request_id":"req-1","payload":{"type":"ask_followup","payload":{"sub_question_id":"...","question":"..."}}}
{"v":"1.0","type":"cancel","request_id":"req-2","payload":{"reason":"...","keep_partial":true}}
```

应答（两类指令共用同一对帧名，与前端 `InterveneAckEnvelope`/`InterveneErrorEnvelope` 一致）：

```jsonc
{"v":"1.0","ts":1757800000000,"type":"intervene.ack","request_id":"req-1","payload":{"status":"running"}}
{"v":"1.0","ts":1757800000000,"type":"intervene.error","request_id":"req-2","payload":{"code":"INTERVENE_NOT_ALLOWED","message":"..."}}
```

- 成功 `payload.status` 即 run 当前状态（cancel 成功为 `cancelled`；不在 WS ACK 带回 partial_report_id）。
- 错误码就是业务判别码本身（无 HTTP 层 `details` 嵌套）：`INTERVENE_NOT_ALLOWED` / `INVALID_ACTION_PAYLOAD` / `RUN_NOT_RESUMABLE` / `not_found` / `auth_error` / `UNKNOWN_COMMAND` / `internal_error`。
- 缺少 `request_id` 的消息被忽略（无关联键，不回帧、不断连）；未知 type 回 `UNKNOWN_COMMAND`；指令处理异常回 error 帧且**不断开连接**；非法 JSON 丢弃保活。
- WS 指令不带来超出 REST 的能力，仅省一次 HTTP；双发竞态由同一 service + 条件 UPDATE 状态机收敛。

### 3.2 实时帧增量

| 帧 type | 时机 | payload | 与前端核对 |
| --- | --- | --- | --- |
| `interrupt.requested` | clarify 挂起落库后、`run.finished(paused)` 前 | `{reason:"clarify", questions[], defaults, expires_in_seconds}`，question 项 `{key,text,options,recommended}` | 与 `InterruptRequestedPayload` / `ClarificationQuestion` 一致 |
| `run.finished`（复用） | pause/cancel 协程收尾 | 既有 RunFinishedPayload，`status` 新增合法值 `"paused"` / `"cancelled"`（前端联合类型已含） | 已含，无新帧 |
| `sub_question.created/started/finished`（复用） | followup 补查 | 既有 M2-4 形态 | 已含 |

不新增 `run.paused` / `run.resumed` 独立帧：paused 由 `run.finished(paused)` 承载（M2-2 冻结口径），resume 后续跑由 `stage.started` 表达。`interrupt.requested` 不做服务端回放，刷新恢复走 §2.6 的 REST `interrupt` 字段。

## 4. 前端对接要点

### 4.1 无需改类型（已逐字段核对，差异 0）

- `PauseRunRequest` / `CancelRunRequest` / `HumanInput`（answers/action/kind）/ `InterventionAction`（type 枚举 ask_followup/exclude_evidence）/ `RunControlResponse`（run_id/status/partial_report_id?）与 openapi-m2-5 一致；
- `ClientCommand` / `InterveneAckEnvelope` / `InterveneErrorEnvelope` / `CommandAck` 与 §3.1 一致；
- `InterruptRequestedPayload`、`ClarificationQuestion` 与 §3.2 一致；
- `RunResponse` 需在前端类型补可选 `interrupt?: InterruptRequestedPayload | null`（若刷新恢复澄清卡走该字段；以本地实际声明为准核对，mock 先行未声明则补可选，不破坏既有消费）。

### 4.2 行为接线提醒

- 四个 mock 模拟端点切真实 HTTP 客户端（`services/api/`），错误映射读 HTTP 体 `details.code`；WS 指令错误读 payload.code（两处码名相同、层级不同）。
- pause/cancel 按钮的最终态以 `run.finished` 帧收敛，不要只以 200/ACK 翻转终态 UI；暂停后阶段行仍 running 是预期。
- WS 握手现在可能因归属被 1008 拒（含 token 用户被注销），重连/错误提示沿用既有通道关闭处理。
- 剔除证据为**单向**操作：本包不提供恢复接口（mock 中 `excluded:false` 的恢复语义为 mock 先行形态，真链不要调用）。
- `expires_in_seconds` 仅展示；澄清答案必须真实提交，不提供「按默认继续」按钮（空答案后端 422）。

## 5. 后端可复现证据

- 离线单测：`test_run_registry.py`（注册表信号/模式升级）、`test_runs_control_api.py`（四端点状态机矩阵、resume 三分支、介入阶段/归属/幂等）、`test_interventions_consume.py`（fan-out 边界消费）、`test_ws_commands.py`（AC-11：ACK/error/request_id/握手归属拒绝/UNKNOWN_COMMAND）、`test_ws_stream.py`（归属校验注入）、`test_audit_logger.py`（AC-12 审计写入失败不阻断）、`test_await_human.py` / `test_executor.py`（澄清回流与 interrupt.requested、协作式取消收尾）。
- 真库集成：`tests/test_integration_m25.py` 3 用例，独立 schema `m25_orm` / `m25_migration`，setup/teardown 各一次 `DROP SCHEMA ... CASCADE`：五控制动作真实落 `audit_entries`（action/payload/target）、JSONB 往返与 `(run_id,idempotency_key)` 唯一索引（NULL 并存）、0004 upgrade/downgrade 表与唯一索引随迁移增减（AC-10/AC-12/AC-14/AC-17）。
- 全量门禁：`pytest -q` **493 passed**（含 7 个真库集成，无跳过）；本批改动文件 ruff check/format 全净；`mypy app` 相对 dev 基线零新增（57 个历史错误为既有债务）。
- 契约：`uv run python -m app.export_openapi` 导出 24 路径；M2-4 快照自本包起默认冻结（重写加 `--refresh-m24`）。

## 6. 联调自测清单（前端回归时勾选）

**类型与构建**

- [x] `RunResponse.interrupt` 可选字段补齐后 `npm run typecheck` 通过；`npm run lint` / `npm run build` 无新增告警。
- [x] 四端点与 WS 指令均切真链（`VITE_MOCK=off`），mock 网关保留供无后端开发。

**真实链路（重点 AC-1~AC-5、AC-8、AC-9、AC-11）**

- [x] AC-1 pause：running 点暂停 → 200 paused，2s 内 GET 收敛 paused；重复 pause 409 `RUN_ALREADY_PAUSED`（响应体 `{code:"conflict",details:{code:"RUN_ALREADY_PAUSED"}}` 与 §2.1 信封一致）；终态 pause 409 `RUN_NOT_PAUSABLE`。`run.finished(paused)` 末帧由 `_finalize_controlled_stop` 发布（前端状态以 GET 收敛为准，未单独抓帧计数）。
- [ ] **AC-2 resume：发现阻塞缺陷，未通过**——200 响应后图不再驱动、run 永久停留 running（详见 §9.1，所有 resume 分支受影响）。
- [x] AC-3/AC-4 cancel：running 取消 200 且 GET 收敛 cancelled；重复 cancel 幂等 200 cancelled；cancel succeeded run 返回 200 status=succeeded 不改状态（partial_report_id 本批取消点均无草稿，字段缺省符合 §2.3）。
- [ ] **AC-5 澄清联通：前半段通过、末段被 §9.1 缺陷阻塞**——真 LLM 宽泛问题（"人工智能行业"）触发 paused@clarify；`GET /runs/{id}` 的 interrupt 含 reason/questions×2/expires_in_seconds=900；空 body 与 kind=proceed 均 422；带 answers resume 返回 200 running；**浏览器整页刷新后澄清卡/抽屉由 REST interrupt 完整恢复（前端本次改动点，实测通过）；UI 提交答案 toast 成功、抽屉关闭**；但恢复后执行同样卡在 running/clarify，无法验证"一路 succeeded、interrupt 转 null"。
- [ ] AC-6 成本挂起：90% 闸门挂起在 M2-4 已实测（danger/paused/snapshot），但"空 body resume → succeeded"走同一 resume 通道，被 §9.1 阻塞，待缺陷修复后复测。
- [ ] **AC-8 ask_followup：窗口问题待确认，成功路径未真链点到**——实测 deep run 的 retrieve 超步仅约 8ms（03:16:50.983→.991，fan-out 异步分发，实际检索/标准化在 standardize 阶段），`_ASK_STAGES=("retrieve",)` 使人类与 HTTP 轮询（100ms/60ms 轮询、40 并发轰炸各一轮）均无法命中，前端"追加追问"按钮真实可用窗口几乎为 0；422/404 的 HTTP 实测被阶段闸门（409）先行拦截。错误信封与 200 受理以后端 `test_runs_control_api.py`/`test_interventions_consume.py` 为准，建议后端确认是否应放宽受理窗口（见 §9.2）。
- [x] AC-9 exclude_evidence：standardize 阶段剔除 200 且同步生效（默认分页即时不含、include_excluded=true 带出且 excluded_by_user=true）；跨 run 证据 404；succeeded(report) 阶段剔除 409 `INTERVENE_NOT_ALLOWED`；非法载荷在终态被 409 先行拒绝（载荷校验以单元测试为准）。
- [x] AC-10 幂等：exclude 同一 Idempotency-Key 双击均 200 且仅一条生效（列表口径一致）；followup 同键双击受 §9.2 窗口所限未 HTTP 点到（幂等查询逻辑与 exclude 同一 service 分支）。
- [x] AC-11 WS：Node WS 客户端实测 6/6——intervene 错误动作回 `intervene.error/INTERVENE_NOT_ALLOWED` 后连接仍 OPEN；`pause` 指令回 `intervene.error/UNKNOWN_COMMAND`；缺 request_id 无回帧；cancel 指令回同 request_id `intervene.ack{status:"cancelled"}` 且 GET 收敛 cancelled。非创建者 1008 拒绝需第二账号，以后端 `test_ws_commands.py`/`test_ws_stream.py` 为准。
- [x] AC-12 审计（只读抽查，可选）：前端无审计 UI，以后端真库集成用例（493 passed）为准。

**回归**

- [x] M2-4 看板：介入动作过程中 stages/sub-questions/evidence/cost 快照可读不回归；证据剔除后看板默认列表即时收敛（total 34 中被剔项消失）。
- [ ] M2-2 裁决：本批真实 run 无 high 冲突（conflicts=0），无真链样本；裁决 UI 在 M3、相关前端代码本批未改动，建议后端按需抽验。
- [x] mock gateway 模式下既有页面行为不回归：demo_full 看板（阶段/is-danger 变色/无错误态）、blocks 报告正常；mock 证据恢复列按交接单 §4.2 门控仅在 gateway 模式渲染（真链剔除单向，见前端改动说明）。

## 7. 前端回归结论（2026-09-14 回填）

### 7.1 改动清单（前端代码 3 文件，+32/-7；本单文档另计）

| 文件 | 改动 |
| --- | --- |
| `src/services/api/types.ts` | `RunResponse` 补可选 `interrupt?: InterruptRequestedPayload \| null`；以 type-only 自 `services/realtime/types` 引入同形类型，无运行时循环依赖（交接单 §2.6/§4.1 唯一必补类型） |
| `src/composables/useRunStream.ts` | 新增 `applyInterrupt()`：GET 详情携带 `interrupt` 时恢复澄清卡（questions/defaults/expires_in_seconds，stage 兜底取 current_stage），显式 null 且非 paused 时清空帧残留；统一挂在 `applyRun` 内调用，替换重试回调中原有单行清空。`interrupt.requested` 服务端不回放，刷新恢复只走该 REST 字段 |
| `src/views/cockpit/CockpitView.vue` | 新增 `canRestoreEvidence`（仅 `VITE_MOCK=gateway` 为真）：已剔除证据「可恢复列」与含剔除项的全量列表拉取均加该门控，真链剔除单向（交接单 §4.2），不调用任何恢复接口、不放大列表请求；模板注释同步标注 |

未改动但已逐路径核对：`services/api/interventions.ts` 四端点本就是真实 HTTP 客户端（`VITE_MOCK=off` 经 Vite 代理到后端，gateway 模式才由 mock 拦截），错误映射读 HTTP 体 `details.code`；WS `sendCommand` 指令信封、ACK/error 帧处理与 §3.1 一致，无需改动。

### 7.2 实测结果（VITE_MOCK=off，真实后端 + 真实 LLM/博查检索）

环境：后端 http://localhost:8000（docker-compose.min.yml PG，0004 已 upgrade，uvicorn --reload），前端 `npm run dev` + `VITE_MOCK=off`，账号 dev@example.com / Dev@123456；quick/deep 多轮真实 run，HTTP 用 PowerShell UTF-8 字节流脚本、WS 用 Node `ws` 客户端（m25_ws.mjs，6/6）。

- **通过**：AC-1 pause 矩阵（running 200→GET 2s 内收敛 paused、重复 pause 409 `RUN_ALREADY_PAUSED`、终态 409 `RUN_NOT_PAUSABLE`，信封 `{code:"conflict",details:{code}}` 一致）；AC-3/AC-4 cancel（running 取消收敛、终态幂等 200 不改状态、succeeded run cancel 返回 succeeded）；AC-9 exclude_evidence（standardize 窗口 200 同步生效、默认分页即时剔除、include_excluded 带出 excluded_by_user、跨 run 404、succeeded 阶段 409 `INTERVENE_NOT_ALLOWED`）；AC-10 同 Idempotency-Key 双击仅一条生效；AC-11 WS 六连测（error 帧不断连、UNKNOWN_COMMAND、缺 request_id 静默、cancel ACK 收敛）；AC-5 前半段（真 LLM 宽泛问题触发 paused@clarify、interrupt 字段两问/900s、空 body 与 proceed 双 422、带 answers 200、**整页刷新后澄清卡由 REST interrupt 完整恢复、UI 提交 toast 成功抽屉关闭**——本批前端改动点实测通过）。
- **回归**：M2-4 看板（介入过程中 stages/sub-questions/evidence/cost 快照全程可读，剔除后默认列表 total 即时收敛）；mock gateway（demo_full 看板、blocks 报告、is-danger 变色、mock 恢复列仅 gateway 渲染）均不回归。
- **门禁**：`npm run typecheck`、`npm run lint`、`npm run build` 全绿，无新增告警。
- 自测清单 15 项勾选：10 项通过；AC-2/AC-5/AC-6 被 §9.1 阻塞（AC-5 前半段通过、末段阻塞，已在条目注明），AC-8 受 §9.2 所限成功路径未点到，M2-2 无 high 冲突真链样本；AC-12 审计为前端只读抽验，以后端 493 passed 真库用例为准。

### 7.3 结论

前端侧对接代码、门禁、可自动化的 REST/WS/错误路径与 mock 回归均完成，契约零字段差异；**因 §9.1 P0 缺陷，resume 全分支在真链不可用（含 M2-4 成本闸门恢复路径），本单不具备关闭条件，不归档**，请后端修复 §9.1、裁定 §9.2 后通知前端，前端用真链重测 AC-2/AC-5/AC-6/AC-8（含 followup 双击幂等与 fan-out 消费帧），全过后双方按 §8 关闭归档。

## 8. 关闭与归档闭环

1. 前端完成 §6 回归后回填本单「前端回归结论」小节（改动文件、实测证据、门禁结果，格式参照 M2-3/M2-4 交接单 §8）；
2. 后端按需抽验无法自动化的浏览器侧项；双方无异议后文首状态改为「**已回归关闭**（YYYY-MM-DD）」，移入 `docs/feedback/archive/`；
3. 回归中若发现后端缺陷：不移动文件，在本单追加「联调问题」小节或提命名带责任方向的缺陷反馈单，修复重测后再关闭；
4. 不在本包的诉求（revert_stage/mark_doubt 与报告后剔除 M4、子问题计划 gate、服务端澄清超时自动续跑、pause/resume 的 WS 指令、取消按钮入口节奏、跨进程控制、孤儿 run 治理 M2-8）以阶段方案 §1.1 为准，勿在前端临时扩包。

## 9. 联调问题（前端提出，后端负责）

### 9.1 【P0 阻塞】resume 返回 200 后图不再驱动，run 永久停留 running（AC-2/AC-5/AC-6，所有 resume 分支必现）

**现象**：paused run 调 `POST /runs/{id}/resume`（澄清 answers 分支、空 body/proceed 分支均同）HTTP 200 `{status:"running"}`，之后无任何 stage 帧、`token_used` 不增长、`current_stage` 不推进、run 永不到终态；UI 上点「继续」toast 成功且抽屉关闭，看板永久 running，只能手动 cancel 收口。

**根因（代码级，时序必现，非竞态概率问题）**：

1. `app/services/runs_control.py` 的 `resume_run` 在 224-228 行先执行条件 UPDATE `status: paused→running`，242 行 `commit()`，245-255 行才 `asyncio.create_task(resume_research_async(...))`；
2. `app/orchestrator/executor.py` 的 `resume_research_async` 在 908-923 行重新加载 run，守卫为 `if run.status != "paused": log.warning("非 paused 状态忽略恢复请求"); return`；
3. service 已提交 running，executor 任务首次读到的必然是 running，守卫恒命中后直接 return——939 行 `aupdate_state` 写 human_input 与 949 行 `_drive_to_terminal` 永远不执行。service 层条件 UPDATE 与 executor 层 paused 守卫对同一 run 做了互斥假设，组合到一起恢复链 100% 断开。

**测试缺口（493 passed 未覆盖该组合链）**：

- `tests/test_runs_control_api.py:359-364` 与 `tests/test_integration_m25.py:301` 都把 `app.orchestrator.executor.resume_research_async` monkeypatch 成 `_no_resume`，API/集成测试不驱动真实 executor；
- `tests/test_executor.py` 的 resume 用例直接调用 executor，调用时 run 仍为 paused（941 行由 executor 自己翻 running），绕过了 service 预翻转；
- 即「service 条件 UPDATE → 真实 executor 续跑」的端到端组合零用例覆盖，建议修复时补一条不 patch executor 的集成用例（answers / proceed 两分支至少各一）。

**实测证据**：

| 分支 | run_id（dev@example.com） | 证据 |
| --- | --- | --- |
| clarify answers | `01M2EXTN56TKXPM5K3YV3G5AGK` | 2026-09-14T03:02:07.190819Z 后端日志「研究运行已调度恢复」（reason=clarify），同刻 POST /resume 200；**3ms 后 03:02:07.193526Z** executor warning「非 paused 状态忽略恢复请求」status=running；此后仅 GET 轮询无任何阶段帧；同 run 前两次空 body/proceed resume 均正确 422（校验链本身无问题） |
| proceed（手动软暂停空 body） | `01M2F0A9T8VMH2SM5FKADCGW6P` | 2026-09-14 本次回归现场复现：quick run running 后 pause → GET 0.5s 收敛 paused（stage=clarify）→ 空 body POST /resume 200 running；连续 10s 轮询 status 恒 running、stage 恒 clarify、token_used 恒 0/50000；03:45:44Z 由前端脚本 cancel 收口（DB 记录 started 03:45:33Z、finished 03:45:44Z、全程 token_used=0、interrupt=null）。复现脚本留存于本机 `tmp/m25_repro_resume.ps1`（gitignored，不随仓库提交） |

**影响面**：AC-2 resume 全流程、AC-5 澄清末段（答案回流后一路 succeeded、`interrupt` 转 null）、AC-6 成本闸门「空 body resume → succeeded」恢复路径（M2-4 已实测的挂起在 M2-5 后无法恢复，属回归阻断）；WS 无 resume 指令不受独立影响。

**修复方向（供后端参考，不指定实现）**：

- 方向 A：executor 守卫不再以 `status=="paused"` 为唯一判据——并发双击已由 service 的条件 UPDATE（`WHERE status='paused'`，affected_rows 抢占）承担，executor 侧重入/重复恢复改由 run registry 在途句柄判重；
- 方向 B：service 不预翻转 running，由 executor 守卫通过后自行落库（executor 941-944 行已有该写入），但需重排 200 响应口径与暂停窗口的并发语义；
- 二选一后务必补上述组合链集成用例，避免再次出现单测全绿、真链全挂。

### 9.2 【P1 待裁定】ask_followup 仅 retrieve 可受理，真链窗口约 8ms，人类与 UI 不可达（AC-8 成功路径无法关闭）

**现象**：真链 `POST /intervene {type:"ask_followup"}` 无论人工点击、100ms/60ms 定时轮询还是 40 并发轰炸，全部收到 409 `INTERVENE_NOT_ALLOWED`；前端「追加追问」按钮门控与契约同为 `current_stage==='retrieve'`（`src/views/cockpit/CockpitView.vue:222-224`），按钮真实可渲染窗口同样约 8ms，人类不可点。载荷 422 与引用 404 分支被阶段闸门先行拦截，未点到（以后端 `test_runs_control_api.py` 单测为准）。

**根因（语义错配，非崩溃）**：`app/services/interventions.py:31` `_ASK_STAGES=("retrieve",)`、120 行闸门；但真链 retrieve 超步只做 fan-out 异步分发，真实检索与标准化发生在 standardize。实测 deep run 六阶段起止（stage 采样，UTC 2026-09-14T03:16）：

| 阶段 | 起止 | 耗时 |
| --- | --- | --- |
| clarify | 34.012 → 49.603 | 15.6s |
| decompose | 49.603 → 50.983 | 1.38s |
| **retrieve** | **50.983359 → 50.991951** | **8.6ms** |
| standardize | 50.991 → 05.813 | 14.8s |
| critique | 05.813 → 05.827 | 14ms |
| report | 05.827 → 05.830 | 3.6ms |

对照：exclude_evidence 窗口为 retrieve/standardize/critique，AC-9 在 standardize 约 15s 窗口内 200 同步生效，问题仅出在 ask_followup 的窗口定义。

**请后端裁定**：ask_followup 的受理窗口是否应放宽到 fan-out 子问题实际仍在检索的区间（候选：retrieve+standardize，或让 retrieve 阶段语义延伸至分发子问题全部回收）。裁定后需同一变更原子同步：冻结契约 `docs/contract/openapi-m2-5.json` 端点描述、本单 §2.2 允许矩阵与 §2.5 消费时机表述、前端 `followupAvailable` 门控（约 1 行 + mock 对应）。裁定前 AC-8 成功路径（200 受理 → 队列 pending → fan-out 边界消费 → `sub_question.created/started/finished` → 证据进同一份报告）与 followup 同 Idempotency-Key 双击的 HTTP 实测均无法关闭（幂等查询与 exclude 同 service 分支，逻辑以后端 `test_interventions_consume.py` 为准）。

---

## 9-补. 后端修复回执（2026-09-14）

### 9.1 修复：resume 恢复链打通（采用前端建议的方向 A）

**修复内容**（`app/orchestrator/registry.py`、`app/services/runs_control.py`、`app/orchestrator/executor.py`）：

1. `RunRegistry` 新增**恢复预留**：`resume_run` 在条件 UPDATE 前同步写入恢复占位并接管可能仍在收尾的首跑协程句柄；重复恢复（已存在恢复占位/恢复协程）409 `RUN_NOT_RESUMABLE`，抢占失败自动还原旧句柄。
2. `resume_research_async` 守卫由「仅认 paused」改为接受 `running`（恢复服务预翻转路径）/`paused`（裁决等直连路径），终态/pending 仍忽略；run 行为仅在加载为 paused 时由执行器翻转，消除「service 已置 running、executor 恒命中忽略」的互斥死结。
3. 微窗口收敛：pause/cancel 落在恢复占位上的信号由恢复协程注册时**沿用并补发 cancel**；恢复协程先等待被取代的首跑协程退出再加载 run；首跑/恢复两协程的取消收尾均改为**属主检查**（句柄已被接管时静默退出、不落库不发帧、不误注销），避免两个驱动循环并发与状态互相覆盖。

**补齐测试缺口**（交接时要求的组合链，不 patch executor）：

- `tests/test_integration_resume.py` 新增 2 条真库端到端：澄清 answers 经恢复服务→真实 executor 一路 succeeded；90% 成本闸门挂起后空 body resume→succeeded（帧序均为 paused→succeeded）。
- `tests/test_run_registry.py` 新增 4 条：恢复占位、占位接管沿用停止信号并补发 cancel、重复抢占还原、属主感知注销。
- 全量 **499 passed**（原 493 + 新增 6），本批 ruff 全净、mypy 与基线一致零新增。

### 9.2 裁定：受理窗口维持「仅 retrieve」，根因是阶段戳记滞后，已修

**裁定结论：不放宽到 standardize/critique。** followup 的唯一消费者在 fan-out 节点的层边界，standardize 时受理的 200 请求没有任何消费者（fan-out 已返回），会造成「受理成功但永不补查」的更差语义。

**实测 8ms 窗口的真正根因**（与前端采样一致的机制解释）：执行器此前只在 LangGraph values 快照（节点执行**完成后**）到达时才写 `run.current_stage` 与阶段行，导致 `current_stage='retrieve'` 的可见区间错位为「retrieve 已完成 → standardize 完成」，真实 fan-out gather（bocha 检索，约 1.4s/层）期间 DB 里仍是 decompose。

**修复**：`_astream_and_publish` 改为 `stream_mode=["debug","values"]` 混合流——在 debug `task` 事件（节点**开始执行前**）即打开阶段行、置 `current_stage` 并提交：

- `current_stage='retrieve'` 现在与 fan-out 真实检索区间一致（单层约 1~2s，多层 followup 循环期间持续开放）；60ms 轮询与人类点击均可命中，阶段时间线/`duration_ms` 同时修正为节点真实耗时（M2-4 看板准确性一并受益）。
- **契约零变化**：`openapi-m2-5.json` 不改，§2.2 允许矩阵与 §2.5「层边界消费」表述维持原样；前端 `followupAvailable` 门控（`current_stage==='retrieve'`）**无需改动**，按钮真实可点窗口即随修复生效。
- resume 回流重跑同阶段（clarify）不重发 `stage.started`；failure_recovery/await_human/user_intervention/cost_checkpoint 不产生六阶段帧。

### 9.3 前端复测清单（修复后）

- [ ] **AC-2**：手动软暂停后空 body resume，200 后 stage/token 持续推进至 succeeded（不再卡 running/clarify）。
- [ ] **AC-5**：澄清 answers resume 后一路 succeeded；终态 `GET /runs/{id}` 的 `interrupt` 为 null。
- [ ] **AC-6**：90% 成本挂起后空 body resume→succeeded（M2-4 挂起链路恢复）。
- [ ] **AC-8**：检索进行中（证据帧持续到达的 1~2s/多层窗口）点「追加追问」200 入队，随后收到 `sub_question.created/started/finished`，补查证据进同一份报告；followup 同 Idempotency-Key 双击仍仅一条。
- [ ] 回归观察：阶段时间线 `duration_ms` 分布变化（retrieve 变为秒级、standardize/critique 回归毫秒/LLM 真实耗时），确认前端无按旧错位时序写死的逻辑；WS 重连与帧序无异常。
