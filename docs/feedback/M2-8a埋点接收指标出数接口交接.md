# M2-8a 埋点接收与指标出数 · 前端对接交接单

> 提出方：后端开发
> 日期：2026-09-15
> 接收方：前端开发（WP-18 telemetry 真链回归）
> 关联里程碑：M2-8a（埋点接收与指标出数），验收 PRD §11 A8（埋点可出数），AC 见阶段方案 §9
> 机读契约：`docs/contract/openapi-m2-8.json`（**27 路径**，在 m2-7 25 路径上新增 2 个 telemetry 端点）
> 阶段方案：`docs/design/AI研究者助手-M2-8埋点接收指标出数与Celery接管技术方案.md`（a 段 v1.0 已冻结，§13 含落地与门禁；b 段 Celery 接管另行交付）
> 状态：**待前端真链回归**——前端 WP-18 运行代码零改动，真实端点已就绪，前端按 §4 确认上报从 404 静默变为 204 落库、控制台不再 warn 后回填结论，关闭归档。

---

## 0. 本次对接结论（TL;DR）

1. WP-18 的 telemetry 模块（telemetry.ts 队列/批量/keepalive、api/telemetry.ts 的 `/telemetry/batch` 调用）**运行代码零改动**：真实端点已上线，路径/请求体/204 语义与 mock 先行时完全一致。
2. 当前真链现象（前端实测 console warn、端点 404）应消失：批量上报返回 **204**，事件落入 `telemetry_events`。
3. 新增一个**只读指标端点** `GET /telemetry/metrics`（内部观察 A8 用，WP-18 无对应 UI，不在本批前端范围；如后续运营页要接，类型见 §2.3）。
4. 无 WS 变化；无前端类型破坏（入站字段与 `TelemetryEvent`/`TelemetryBatchRequest` 同构）。
5. 联调先执行 **`alembic upgrade head`（head 0005 → 0006）**；新配置均有默认值，无需额外环境变量。

## 1. 联调环境

后端 http://localhost:8000（docker-compose.min.yml PG，alembic head=**0006**）、前端 `npm run dev` + `VITE_MOCK=off`、账号 dev@example.com / Dev@123456。**8a 不引入 Redis/worker**（那是 8b），仍是单 uvicorn 进程。

新增配置（`.env.example` 已同步，默认即可）：

| 变量 | 默认 | 前端是否关心 |
| --- | --- | --- |
| `TELEMETRY_SAMPLE_RATE` | 1.0 | 否；默认全采，0 时端点仍 204 但不落库（调试用，联调保持 1.0） |
| `TELEMETRY_RATE_LIMIT_PER_MIN` | 12 | 仅在异常高频上报时触发 429；正常节奏（5 条/批 + 10s 兜底）远低于此 |

## 2. 契约明细（openapi-m2-8，27 路径）

### 2.1 `POST /api/v1/telemetry/batch`（前端唯一需要关心的端点）

- 请求体：`{ "events": TelemetryEvent[] }`，每事件 `{event, ts, run_id?, page?, props?}`，与 WP-18 现有发送形态逐字段一致。
- 成功：**204 无响应体**；响应头 `X-Accepted-Events: <n>` 仅供后端调试，前端不要依赖。
- 批级 422：`events` 不是数组/空数组/超过 200 条/缺 event 或 ts/类型错误（FastAPI 422，body `detail`）；raw body 超 64KB 返回 422 `{code:"telemetry_batch_invalid"}`。WP-18 单批最多 5 条，正常不会命中。
- 超频：429 `{code:"telemetry_rate_limited"}`（>12 批/用户/分钟）。
- **条级容错（关键，与前端队列重试的关系）**：单条事件 event 非法字符、ts 超出 ±7 天、page/run_id 超长、props 含嵌套对象/数组/null/超 256 字字符串/键超限等，**只丢该条，其余照收，整批仍 204**；全部非法也 204。前端无需为单条坏数据做任何处理，坏队列不会因 422 卡死反复重试。

### 2.2 props 隐私白名单

只收扁平标量（string/int/float/bool）；键名 `^[a-z0-9_]{1,64}$`、≤20 键、字符串值 ≤256 字。WP-18 现有三事件 props（intent 枚举/耗时/degrade_reason、intervene 的 action/result/error_code、citation 的 evidence_id）全部合规。**不要**往 props 加问题原文、澄清答案、token 等自由文本——会被服务端整行丢弃。

### 2.3 `GET /api/v1/telemetry/metrics?from=&to=`（本批前端无 UI）

登录可读，返回窗口内全局聚合标量（无明细/无 user/team 维度），默认近 30 天、最大 90 天。结构（供后续运营页参考，WP-18 不消费）：

```jsonc
{
  "window": {"from": "...", "to": "..."},
  "runs": {"total_terminal": 42, "succeeded": 36, "failed": 2, "cancelled": 4, "success_rate": 0.947},
  "intervention": {"runs_started": 50, "runs_with_intervention": 11, "intervention_rate": 0.22,
                   "by_action": {"pause": 3, "resume": 1, "followup": 2, "exclude": 4, "clarify": 1}},
  "traceability": {"final_reports": 36, "avg_numeric_binding_rate": 1.0,
                   "runs_with_citation_open": 30, "citation_open_rate": 0.833, "citation_open_events": 214},
  "intent": {"classify_total": 130, "degraded": 5, "degraded_rate": 0.038},
  "ingest": {"accepted_events": 1520, "dropped_events": 3, "rate_limited_batches": 0}
}
```

口径要点：成功率分母 succeeded+failed（cancelled 排除、无样本为 null）；介入率分子只信业务表（run_interventions applied + run.pause/run.resume 审计按 run 去重），`clarify` 计数取自前端 cockpit.intervene 成功事件（澄清提交复用 resume 通道，业务表无法拆分）；溯源率为 final 报告机器绑定率均值 + report.citation.open 的 distinct run 交互口径双值。

## 3. WS 帧

无增删改；取数/上报与 WS 无关。

## 4. 前端真链回归清单

- [ ] 迁移后发起真实 run：意图分类后 `intent.classify` 上报 **204**（此前 404）；控制台不再出现 `/telemetry/batch` 404/网络错误 warn（DEV 下 `[telemetry]` 预览 warn 是 telemetry.ts 自身的本地预览日志，属正常，区别在 Network 面板状态码）。
- [ ] 看板执行 pause/resume/followup/exclude/clarify：对应 `cockpit.intervene` 批量上报 204。
- [ ] 报告页点击角标：`report.citation.open` 上报 204。
- [ ] 断网/刷新：keepalive 与 localStorage 补发链路行为不回归（服务端条级容错保证坏条不卡死队列）。
- [ ] `npm run typecheck/lint/build` 全绿（预期零代码改动；若有类型不匹配以 openapi-m2-8 为准反馈）。
- [ ] 回归：报告/看板/意图主链路不受 telemetry 端点影响（埋点是旁路，失败静默不阻塞主流程）。

## 5. 关闭闭环

1. 前端完成 §4 确认真链 204 落库（可由后端配合查 `telemetry_events` 行数或提供一次 metrics 截图）后回填本单；
2. 双方无异议后文首状态改「已回归关闭」并移入 `docs/feedback/archive/`；
3. metrics 运营可视化、商业指标、审计明细列表不在本包，流转 M3/M6。

## 6. 后端可复现证据与门禁

- 新增：`app/telemetry_ingest/`（滑窗限流/稳定采样/计数）、`app/services/telemetry.py`（两级校验/聚合）、`app/schemas/telemetry.py`、`app/api/v1/telemetry.py`、`app/db/models/telemetry.py`、0006 迁移。
- 单测：`test_telemetry_ratelimit.py`（4）、`test_telemetry_api.py`（11，含 204/422/429/采样/条级清洗/未登录）；真库 `test_integration_m28.py`（4：指标聚合四象限、空窗口 null、窗口校验、0006 迁移循环）。
- 全量门禁：**655 passed**（基线 636 +19，含全部真库集成零跳过）；ruff 全净；mypy 54 条相对基线零新增。
- 契约：openapi-m2-8 27 路径；M2-7 快照 25 路径零 diff（转默认冻结）。
