# M2-8b 长任务接管（Celery + Redis 跨进程） · 前端对接交接单

> 提出方：后端开发
> 日期：2026-09-15
> 接收方：前端开发（三进程真链回归）
> 关联里程碑：M2-8b（Celery 接管长任务 / 跨进程事件 / 孤儿清扫），AC 见阶段方案 §19（B-AC-1~B-AC-12）
> 机读契约：`docs/contract/openapi-m2-8.json`（**仍 27 路径，b 段零 REST/WS 增删**）
> 阶段方案：`docs/design/AI研究者助手-M2-8埋点接收指标出数与Celery接管技术方案.md`（v1.0 已冻结，§23 含落地/偏差/门禁）
> 状态：**待前端真链回归**——研究执行已从 API 进程迁到独立 Celery worker，REST/WS 协议对前端透明、前端运行代码预期零改动；前端按 §4 完成三进程真链回归后回填结论，关闭归档。

---

## 0. 本次对接结论（TL;DR）

1. **执行位置迁移，协议不变**：研究首跑/恢复改由独立 worker 进程经 Celery 队列驱动，实时事件改走 Redis Pub/Sub 跨进程扇出到 API 的 WS 端点。前端连接的 `ws://localhost:8000/api/v1/ws/runs/{id}/stream`、全部帧名/载荷、REST 路径与请求体**零变化**。
2. **前端运行代码预期零改动**：`useRunStream`/看板/报告页无须为 b 段改代码。
3. **唯一新增语义**：pause 一个「执行进程已失联、等待自动清扫」的 run 时，409 响应 `details.code` 新增 **`RUN_ORPHAN_RECOVERING`**（HTTP 状态仍 409；M2-8b 前该场景返回 `RUN_NOT_PAUSABLE`）。前端可不识别（走现有 409 提示即可），建议识别后提示「任务执行状态异常，系统正在自动恢复，请刷新后重试」。
4. **联调形态变为三进程**：PG + Redis（docker 栈）与主机上的 **uvicorn + Celery worker 两个常驻进程**。worker 不在线时新 run 会停在 `pending`（任务在队列无人消费），回归前务必确认 worker 已启动（见 §1）。
5. 联调先执行 **`alembic upgrade head`（head 0006 → 0007）**；新增配置均有默认值（`WORKER_ENABLE_REDIS=true` 等），.env 已含 Redis 连接时无需额外设置。

## 1. 联调环境

- 中间件：`docker compose -f docker-compose.min.yml up -d --build`（min 栈**已纳入 redis 与 worker 容器**；回归三进程真实形态也可按 M2-8a 习惯只起 PG+Redis 容器、worker 在主机 WSL 前台跑）。
- 主机 WSL 需同时常驻两个进程（无 --reload；worker 冷启动约 10-15s 属正常，见 §5 观察 1）：

```bash
# 进程一：API（工作目录 backend/）
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
# 进程二：Celery worker（等价 celery worker -l info -Q research,report,ingestion）
uv run deep-research-worker
```

- worker 就绪标志：日志出现 `celery@<host> ready` 且任务清单含 `research.execute_run`、`research.resume_run`、已连接 `redis://.../1`。
- 账号 dev@example.com / Dev@123456；前端 `npm run dev` + `VITE_MOCK=off`。
- 新增配置（`.env.example` 已同步）：

| 变量 | 默认 | 前端是否关心 |
| --- | --- | --- |
| `WORKER_ENABLE_REDIS` | true | 否。false 退回单进程内存态（仅离线测试）；生产 worker 必须 true |
| `RUN_LEASE_TTL_SECONDS` / `RUN_LEASE_HEARTBEAT_SECONDS` | 30 / 10 | 否。worker 崩溃后约一个 TTL+grace（默认 90s）内 run 被清扫收敛 |
| `RUN_ORPHAN_GRACE_SECONDS` | 60 | 否。判定失联孤儿的宽限 |
| `RUN_ORPHAN_SWEEP_ENABLED` | true | 否。worker 首任务前清扫开关 |
| `CELERY_TASK_MAX_RETRIES` | 2 | 否。Provider 瞬时故障的任务重试上限 |

## 2. 契约影响（openapi-m2-8，仍 27 路径）

### 2.1 REST：路径/请求体/响应零变化

- `POST /runs`、`POST /runs/{id}/pause|cancel|resume|intervene`、全部 GET 端点行为与 M2-8a 一致。
- 唯一差异是 pause 孤儿场景的 `details.code`（见 §2.2）；其余错误码（`RUN_NOT_PAUSABLE`/`RUN_ALREADY_PAUSED`/`RUN_NOT_RESUMABLE` 等）口径不变。
- 响应时序：因任务经队列投递给 worker，`POST /runs` 201 后 run 在 `pending` 停留到 worker 取任务（本机实测约 10-15s 冷启动，热任务更快），随后翻 `running`；前端既有「pending/running 看板等待 + WS/轮询收敛」逻辑天然兼容，无需新增轮询或超时假设。

### 2.2 pause 新增 details.code：RUN_ORPHAN_RECOVERING

| 场景 | HTTP | code | 前端建议 |
| --- | --- | --- | --- |
| running 且 worker 租约新鲜（正常在途） | 200 | - | 既有暂停流程 |
| 非 running / 内存态孤儿 | 409 | `RUN_NOT_PAUSABLE` | 既有提示 |
| running 但执行租约过期（worker 失联，等待/正在清扫） | 409 | **`RUN_ORPHAN_RECOVERING`** | 提示「任务执行状态异常，系统正在自动恢复，请刷新后重试」；不识别时走现有 409 文案也不破坏功能 |

该码只在 worker 崩溃且租约过期（默认 >90s 未心跳）的罕见场景出现，正常回归中可遇而不可求；后端已用真链+单测验证，前端不做专项构造亦可关闭。

## 3. WS 帧

- 帧名、载荷、envelope（v/event_id/ts/run_id/stage/type/payload）、ping/pong、终态即断：**全部不变**。
- 传输路径由「worker 同进程直投」变为「worker → Redis Pub/Sub → API 桥接协程 → WS」；前端无感知。后端真链已验证跨进程下 stage.* / sub_question.* / evidence.fetched / token.usage.update / interrupt.requested / conflict.* / run.finished 全帧不丢不重（同节点本地直投 + 跨节点桥接，origin 去回环）。
- 终态兜底不变：事件偶发丢失仍以 REST 轮询/重连初帧收敛。

## 4. 前端真链回归清单

- [ ] **环境就绪**：min 栈 PG+Redis healthy；主机 uvicorn 与 deep-research-worker 均在线（worker 日志 ready）；`alembic current` 为 0007；`VITE_MOCK=off`。
- [ ] **B-AC-11 全链路**：发起真实 standard run，浏览器 WS 收到完整帧序列（clarify→decompose→retrieve→standardize→critique→report 的 stage.started/finished、sub_question.*、evidence.fetched、token.usage.update、run.finished(succeeded)），看板自动收敛、blocks 终稿可读；run 从 pending→running 的等待属正常（冷启动 10-15s）。
- [ ] **B-AC-2/4 pause→resume**：running 中暂停收到 run.finished(paused) 看板暂停；普通「继续研究」（kind=proceed）恢复并跑到 succeeded；M2-8a 修复的 clarify 手动暂停恢复入口不回归。
- [ ] **澄清真挂起恢复（B-AC-3 窗口口径）**：触发澄清 interrupt.requested 时提交答案恢复，ask_followup 受理窗口与 debug 阶段口径不回退（followup/exclude 窗口物理很短，沿用 M2-8a「脚本/同构旁证」方式即可）。
- [ ] **cancel keep_partial（B-AC-2）**：running 中取消收到 run.finished(cancelled)；有报告草稿时 partial_report_id 语义不变。
- [ ] **重连/刷新**：run 进行中刷新页面，REST 快照 + WS 重连后状态/成本/阶段一致收敛（跨进程桥接不改变初帧/轮询语义）。
- [ ] **（可选，难造）孤儿码**：RUN_ORPHAN_RECOVERING 场景需 worker 崩溃+租约过期，后端真链已验；前端可只确认 409 未知 code 走通用错误提示不白屏。
- [ ] `npm run typecheck/lint/build` 全绿（预期运行代码零改动）。
- [ ] 埋点旁路不回归：M2-8a 的 telemetry 204 照常（telemetry 不经 worker，与本批无关）。

## 5. 关闭闭环

1. 前端完成 §4 真链回归后回填本单（重点取证：跨进程 WS 全帧、pause/resume、cancel）；
2. 双方无异议后文首状态改「已回归关闭」并移入 `docs/feedback/archive/`；
3. 任务级装配冷启动优化、beat 周期清扫、多 worker 水平扩展不在本包，按方案 §1.4/§22 挂后续。

### 5.1 观察项（不阻塞关闭）

1. **worker 冷启动 10-15s**：worker 依赖为任务级装配（方案 §23.2 偏差 2，方案允许的形态），每个 Celery 任务首次建连/导入约 10-15s 后 run 翻 running（WSL drvfs 下 LLM provider 模块首次导入占大头）；连续任务、容器/ext4 部署更快。功能与时序正确性不受影响，前端等待态已有覆盖。进程级常驻 loop 池化为后续优化项。
2. **清扫时机**：worker 在「每个子进程第一个任务执行前」扫一次（非进程启动即刻，方案 §23.2 偏差 1）；即 worker 重启后第一个 run 投递时先收敛历史孤儿再执行。前端唯一可感知面仍是 §2.2 的 409 code。
3. **prefork 重定向日志**：后台文件方式跑 worker 时子进程 structlog 日志可能不显示（MainProcess 的 received/ready 正常）；容器部署 stdout 由 Docker 收集。排查以 run 行 execution_owner/lease_until、Redis 键与 WS 帧为准。

## 6. 后端可复现证据与门禁

- 新增：`app/core/redis.py`、`app/orchestrator/lease.py`、`app/workers/bootstrap.py`、`app/workers/reaper.py`、0007 迁移；改造：`realtime/hub.py`（双态桥接）、`orchestrator/registry.py`（双态）、`orchestrator/executor.py`（超步边界轮询/owner_id/瞬时故障上抛）、`workers/tasks/research.py`（Celery 外壳+心跳+有限重试）、`workers/celery_app.py`（task_routes）、`api/v1/runs.py`、`services/runs_control.py`（双态分派+RUN_ORPHAN_RECOVERING）、`audit/base.py`（run.orphan_recovered）、lifespan/config/.env.example、docker-compose.min/dev。
- 测试：净增 40 个——租约键语义、Hub 跨进程扇出、worker 装配/重试、孤儿三类清扫、executor 控制键/重投递幂等/Provider 上抛、双态注册表、pause 孤儿码、m28b 真库（扫描过滤+真实收敛+0007 迁移往返）。
- 全量门禁：**695 passed**（基线 655，含全部真库集成零跳过）；ruff 本批 32 文件全净；mypy 53 条相对基线 54 条零新增（净减 1）；OpenAPI 27 路径零变化。
- B-AC-11 真链（2026-09-15）：pause→resume→succeeded（run `01M2HR3W8NA20VEMAZ4DQ4C9MB`，跨进程全帧到达）、cancel（run `01M2HR540W5D4KXXJ12BB2WW3R`）、重启清扫（孤儿 `01M28BORPHAN0000000000001` → failed/RUN_WORKER_LOST + 帧 + 审计，触发 run `01M2HRBNJGGJEXGXEPVM5DDVCW`）；历史 pending run `01M2HJZEJTPN9TC9YCN2X2CZ1F` 被重投并自然 succeeded。
