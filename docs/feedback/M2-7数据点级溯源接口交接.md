# M2-7 数据点级溯源 · 前端对接交接单

> 提出方：后端开发
> 日期：2026-09-14
> 接收方：前端开发（WP-16 报告 blocks 引擎/CitationMarker/SourcePanel 真链回归）
> 关联里程碑：M2-7（数据点级溯源），验收 PRD A2（≥10 论断角标回溯 URL+片段）/A3（四类区块），AC 见阶段方案 §9
> 机读契约：`docs/contract/openapi-m2-7.json`（**25 路径**，在 m2-5 24 路径上新增 1 个 citations 端点 + 两报告端点响应超集）
> 阶段方案：`docs/design/AI研究者助手-M2-7数据点级溯源技术方案.md`（v1.0 已冻结，含实现冻结记录 §13）
> 状态：**P1-1 后端已修复（2026-09-15，回执见 §9 末尾），待前端重启 uvicorn 后复测 P1 并补抽真链 dispute，复测通过前暂不归档**——§6 三组跟随项已全部完成、门禁全绿；A2 与四类区块之三类真链通过，dispute 块真链零命中（口径见 §5/§10）。

---

## 0. 本次对接结论（TL;DR）

1. WP-16 的 blocks 引擎、CitationMarker 三态、SourcePanel、三索引、双轨判定**运行层零改动**：后端真实产出已对齐 `types.ts` + `demo_full.ts` 冻结形态，`useReportBlocks` 成功后并行三拉取（报告/citations/conflicts）在真链可用。
2. **新增 1 端点**：`GET /api/v1/reports/{run_id}/citations`（mock 路由同名先行，参数实参为 runId，语义不变）。
3. **两报告端点响应超集**：`GET /reports/{run_id}` 与 `GET /runs/{run_id}/report` 在原八字段上增 `outline` + `blocks`；终稿 `status` 由 draft 纠正为 **final**。
4. **前端有 3 组跟随项**（mock fixture 样例数据 + 过期注释/措辞，非引擎改动，见 §6），随回归批处理。
5. **WS 零变化、不补发 `report.finished`**：真链取数时机是 `run.finished(status=succeeded)` 后（冻结代码 ReportView 已是该口径）。
6. 联调先执行 **`alembic upgrade head`（head 0004 → 0005）**。

## 1. 联调环境

后端 http://localhost:8000（docker-compose.min.yml PG；**alembic head=0005**）、前端 `npm run dev` + `VITE_MOCK=off`、账号 dev@example.com / Dev@123456。

新增配置（`.env.example` 已同步，默认即可）：

| 变量 | 默认 | 前端是否关心 |
| --- | --- | --- |
| `REPORT_LLM_TIMEOUT_SECONDS` | 45 | 否；超时走机械映射降级，终稿结构仍完整 |
| `REPORT_LLM_MAX_TOKENS` | 4096 | 否 |

## 2. 契约明细（openapi-m2-7，25 路径）

### 2.1 新增 `GET /api/v1/reports/{run_id}/citations`

报告级信源索引：按 marker 升序、报告级去重（同一证据跨块共享一个角标，只返回首条片段）。

响应元素 `ReportCitationItem`（线上 **10 字段** = 关联键 + 九展示字段）：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `evidence_id` | 是 | 关联键 |
| `marker` | 是 | 后端格式化的 `[N]`，从 1 起 |
| `snippet` | 是 | 后端取自证据的原文片段（模型不可编写） |
| `url` / `title` | 是 | 回溯目标 |
| `domain` / `source_type` / `source_level` / `credibility` | 否（M2-6 后实际总有值） | SourceBadge 同源元数据 |
| `published_at` | 否 | 可 null |

错误：run 不存在/非属主 404；报告未生成 422 `validation_error`；draft（取消保留的部分稿）返回 **200 空列表**。

### 2.2 两报告端点响应超集

`outline: ReportOutlineItem[]`（`{id,title,type}`，2~4 条）：

- 固定 `sec-overview 调研概述`(conclusion)、`sec-findings 核心发现`(conclusion)；
- `sec-disputes 分歧与不确定性`(dispute) 仅当存在分歧块；
- `sec-limitations 局限与建议`(limitation) 始终至少一条（LLM 未产出时后端兜底）。

`blocks: ReportBlock[]`，与前端类型同构：

| type | 携带字段 |
| --- | --- |
| `conclusion` | id/type/text/claim_id/**confidence**/citations |
| `evidence` | id/type/text/citations（无 claim_id、无 confidence） |
| `dispute` | id/type/text/**claim_id**/**conflict_id**/**confidence**/citations |
| `limitation` | id/type/text/citations（无 claim_id、无 confidence） |

内联 `citations` 元素为三字段 `{evidence_id, marker, snippet}`；`confidence` 三值 `single_source/cross_verified/inferred`（中文标签 WP-16 已实现）。

字段名/枚举/可选性已与 `frontend/src/services/api/types.ts` 逐字段核对，**差异为 0**；block.id 后端终稿恒有值（前端可选声明不拒绝）。draft/历史报告 `outline=[]、blocks=[]`。

## 3. 终稿数据行为（解释异常渲染）

1. **marker 仅被引证据占号**：按材料池顺序（跨子问题、可信度→相关性）为**被引用到的**证据编号，未被任何 block 引用的证据不占号、不在 citations 端点返回；真链信源条数 ≤ 证据总数。与 fixture「全量占号」不同（demo 12 条恰好全被引，输出碰巧一致）。
2. **snippet 两级粒度**：LLM 提供 quote 且在 trafilatura 正文中命中 → 取原句窗口（约 120 字，最细粒度）；未提供/正文缺失/不命中 → 回证据检索摘要。两种情况引用都保留，回溯不损。
3. **100% 绑定硬指标**：含数字/百分比/金额/年份等数据点的 conclusion 无引用时，后端做一次整份自修复；仍无引用的数字块直接从终稿剔除（不冒充事实）；无数字的无来源结论强制降级 `inferred` 保留。内部审计在 `content_json.citation_audit`（不进响应）。
4. **dispute 块后端确定性注入**：待裁决冲突与 both/reject 保留分歧各一块，强制挂双方/存活方证据与 `conflict_id`；双方证据均被用户剔除时该冲突不出块（仍可在分歧列表端点查看）。
5. **终稿 status=final**：取消运行保留的部分稿仍为 draft（旧 content_json 形态、blocks/outline 空、无引文）。

## 4. WS 帧

本包 **WS 零变化**，不新增帧、不补发 mock 剧本里的 `report.finished`。前端取数时机以 `run.finished(status=succeeded)` 为准（ReportView 冻结代码即此口径：succeeded 后拉取、422 时轮询）。

## 5. 前端真链回归清单（A2/A3）

> 回填于 2026-09-14，样本与证据汇总见 §10；抽验脚本留存前端本机 gitignored `tmp/m27/`。

- [x] 发起真实 run（standard，问题覆盖含明确数字的主题），succeeded 后进报告页：**进入 blocks 轨**（不回退 markdown），目录 2~4 条语义章节，区块按真实分布渲染。5 个真实 run（耗时 42~87s）全部进入 blocks 轨；目录实测 3 条（无分歧 run），id 恒为 sec-overview/sec-findings/sec-limitations，有分歧时 sec-disputes 按需位（mock 全分支实测 4 条）。
- [x] **A2**：抽样 **≥10 个带角标的论断**，逐个点击/键盘触发：SourcePanel 打开且 URL 可回溯、片段为来源原文；hover Popover 迷你 SourceBadge 正常；底部信源索引条数 = 最大 marker 序号 = 去重证据数。run `01M2G2641DXT8VVSDQ0HM2K8XW` 浏览器实测正文 25 个角标实例覆盖 [1]~[12]，hover 迷你卡（SourceBadge C/搜索结果/三手来源）、点击与键盘 Enter 开抽屉、Esc/X 关闭、切换定位、外链 https 新窗口均正常；五 run 脚本断言信源条数=最大 marker=去重证据数（14/12/16/10/12）。
- [ ] **A3**：四类区块完整核对；存在 high 待裁决或 both/reject 保留分歧的 run，dispute 块带冲突议题/双方口径/conflict_id，ConflictBlock 只读联动正常。——**真链部分覆盖，保留未勾**：conclusion/evidence/limitation 三类在 5 个真实 run 全量核对通过（字段白名单、枚举、confidence 中文标签色值见 §10）；dispute 块真链零命中（5 run 共检出 3 个冲突，severity 均 low/medium 且被权威规则自动 resolved，0 个 awaiting_human/both 保留），改以 mock demo_full 浏览器实测（block-07 内联只读 ConflictBlock：议题/双方口径/A·D 徽标/conflict_id 联动正常，注：冻结实现为点击红色摘要条的区块内内联展开，非抽屉）+ 后端 `test_report_blocks.py` dispute 注入用例与真库 m27 用例为据；待 §9 P1 修复复测轮顺带补抽真链 dispute。
- [x] 角标行为：跨块同证据同 marker（脚本逐块断言通过）；未被引用证据不占号、不进 citations 端点：run#1 14 索引 < 24 证据、run#2 12 < 19、run#5 14 < 22 点验成立。
- [ ] inferred 块以「推断」样式呈现且不含无来源数字；limitation 章始终存在。——**拆判**：limitation 章 5 run 始终存在（3/3/4/2/3 块）；inferred 项被 §9 P1 阻塞（run#1 block-01 含年份/数量词却零引用），保留未勾。
- [x] 并行三拉取（报告/citations/conflicts）真链 200（每 run 实测）；404（run 不存在，两报告端点+citations）/422（报告未生成，错误体 `{code:"validation_error"}`）口径与 mock 一致；draft 历史报告（30+ 条 M1 旧 run）实测 outline=[]、blocks=[]、citations 200 空列表，cancelled 无报告 run 两端点 422；`npm run typecheck/lint/build` 全绿；真链与 mock 浏览器控制台 0 应用侧报错。
- [x] 回归：M2-5 剔除证据不引用链路由材料池白名单保证（`backend/app/reporting/blocks.py` 装配过滤），本轮 5 run 未产生人工剔除操作（无裁决/剔除窗口），未重新真链取样，以 M2-5 已归档回归 + 后端白名单/dispatch 用例为据；M1 markdown 轨对 draft/历史报告仍可用（实测旧 draft content_md 978 字非空 + blocks=[] 走 markdown 轨），生成中 422→3s 轮询状态卡脚本实测。

## 6. 前端跟随项（mock/文档级，本交接的组成部分）

1. `frontend/src/services/mock/fixtures/demo_full.ts`：outline 改为语义 id/固定标题（sec-overview/sec-findings/sec-disputes/sec-limitations）、分歧/局限章按需出现；`CITATION_MARKERS` 改为仅被引证据占号（citations 端点语义）。demo 数据下两种规则输出碰巧一致，改后核对剧本无视觉回归。
2. 清退过期注释：`types.ts` 约 L430/L440-443 与 `useReportBlocks.ts` 中「M2-7 冻结前/后」「mock 超集」类措辞。
3. 文档措辞：前端详设 §11.4 与 WP-16「report.finished 后切 blocks」改为 **run.finished(succeeded) 驱动**（后端无 report.finished 帧）。

以上均为前端仓改动，后端不在本包代改；如真链发现字段级差异，以 openapi-m2-7 冻结契据为准在本单反馈。

## 7. 后端可复现证据与门禁

- 新增：`app/reporting/`（schemas/prompts/disputes/blocks）；改造 reporter/executor/state/dependencies、`services/reports.py`、`schemas/reports.py`、reports/runs 路由、0005 迁移、config/export_openapi。
- 单测：`test_report_blocks.py`（45，marker/白名单/quote 窗口/缺源降级/数字剔除/dispute 注入/outline/机械映射）、`test_reporter.py`（结构化接线/自修复/降级 +6）、`test_runs_api.py`（超集/信源索引/404/422/draft 空列表 +5）、`test_executor.py`（final+引文落库断言更新）。
- 真库：`test_integration_m27.py`（m27_orm：quote 命中「30%」原句 → final 报告/3 条 block×证据引文 → service 信源索引九展示字段；m27_migration：0005 upgrade/downgrade 往返列/约束/索引）；m24/m25 历史迁移循环用例钉死自身版本。
- 全量门禁：**634 passed**（基线 576，+58，含全部真库集成零跳过）；本批 ruff check/format 全净；`mypy app` 54 条与 dev 基线 54 条零新增。
- 契约：openapi-m2-7 导出 25 路径；m2-5 快照保持 24 路径零 diff（已转默认冻结，重写需 `--refresh-m25`）。

## 8. 关闭闭环

1. 前端完成 §5 抽验 + §6 跟随项后回填本单（真链 run 样例、四类型分布、门禁结果）；
2. 双方无异议后文首状态改「已回归关闭」并移入 `docs/feedback/archive/`；
3. 不在本包的诉求（SSE 报告流、版本化/批注/导出/分享、证据簇、fixture 之外的引擎重构）以阶段方案 §1.1 为准流转 M3/M4。

## 9. 联调问题（2026-09-14 前端真链回归提出，责任方：后端）

### P1-1：兜底概述块绕过数字绑定审计，零引用「推断」结论可携带年份/数量词

**现象（必现路径，概率触发）**
真链 run `01M2G1N5YBTV2G21MFGKA2YPST`（2026-09-14 13:29 UTC succeeded，standard，问题含「2024年…多少万辆…百分之多少」）终稿 block-01 为：

```json
{"id":"block-01","type":"conclusion","claim_id":"claim-01","confidence":"inferred",
 "text":"围绕「2024年全球新能源汽车销量是多少万辆？同比增长率约为百分之多少？渗透率达到多少？不同机构口径有何分歧？」开展的多源核验已形成以下结构化报告。",
 "citations":[],"conflict_id":null}
```

浏览器 getComputedStyle 取证：该块以 conclusion 论断样式 + 灰色「推断」徽标（rgb(92,100,120)/rgb(238,241,245)）置顶渲染，块内 0 角标，文本含年份「2024」与数量词「万辆」。与本交接 §3.3「100% 绑定硬指标」及 §5「inferred 块不含无来源数字」验收口径冲突。

**代码级根因（时序机制）**
`backend/app/reporting/blocks.py` 存在两条进入终稿的路径，数字审计只覆盖第一条：

1. §5.5 审计循环（`assemble_report` L333-355）：无引用 conclusion 命中 `has_numeric_assertion`（L92-99 正则首分支即裸 `\d`，年份必然命中）即剔除——本 run 该轮计数 `dropped_numeric_blocks=1`，LLM 唯一的数字结论在一次自修复后仍被剔除；
2. 剔除后终稿零 conclusion，L377-380 直接 `ordered.insert(0, _fallback_overview_block(question))`；`_fallback_overview_block`（L302-306）把**研究问题原文**嵌进结论（`围绕「{question}」开展…`），产出的是普通 dict 而非 DraftBlock，**不经 §5.5 审计、不占 dropped 计数**，携带数字时原样进终稿并在 L412-461 被分到 claim_id=claim-01、confidence=inferred、citations=[]。

DB 取证（`reports.content_json.citation_audit`，该 run）：`reporter_degraded=false、dropped_numeric_blocks=1、forced_inferred_blocks=1、claim_blocks=1、numeric_claim_binding_rate=1.0`——恒等式自检报 1.0 的同一份终稿里实际存在无引用数字 conclusion，自相矛盾。

**触发条件（非孤例）**：LLM 产出的全部 conclusion 被数字审计剔除（或机械降级无 claim）且研究问题含数字/年份/数量词时必现；数字问题正是 A2 抽验建议的问题形态，5 run 样本中第 1 个即命中。

**测试缺口（为何后端 634 全绿仍漏）**
`test_report_blocks.py` 覆盖了 fallback 概述块的「无结论块时置顶」结构断言，但用例问题文本均为无数字占位串，未把「数字问题 × 零 conclusion 存活」组合喂入 `assemble_report`；`numeric_claim_binding_rate` 在 L478 硬编码 1.0、不随 fallback 路径回算，断言无法发现该洞。

**影响面**
- 卡住 §5 验收项「inferred 块不含无来源数字」，与 §3.3 硬指标及审计恒等式矛盾；前端无缺陷（按冻结形态如实渲染），不建议前端加兜底掩盖；
- 不阻塞 A2 主链路（其余 11 块 14 信源溯源全部正常）；读者可见的是无角标灰色推断句，语义上不冒充具体事实（问题回指而非断言），故定 P1 非 P0。

**供参考的修复方向（不指定实现）**
让兜底概述块与 §5.5 审计同口径：例如问题文本命中数字断言时改用不带问题原文的无数字概述（L305 已有「本次研究的多源核验结果如下。」分支），或将 fallback 块统一过一遍审计/计数并使 `numeric_claim_binding_rate` 反映真实终稿；补「数字问题 + 零 conclusion 存活」组合用例。

**前端复测承诺**：后端修复回执（并重启 uvicorn 加载新代码）后，用同形态数字问题重跑真链，复测本块与 A3 dispute 抽样。

**后端修复回执（2026-09-15，责任方：后端）**

已按上述根因修复，纯 `app/reporting/blocks.py` 引擎纯函数改动，**零契约/零迁移/零端点变化**（openapi-m2-7 仍 25 路径，alembic head 仍 0005，无需重新迁移；但需**重启 uvicorn** 加载新代码）：

1. `_fallback_overview_block`：研究问题文本命中 `has_numeric_assertion`（裸数字/百分比/金额/年份/序数/中文数量词）时不再嵌入问题原文，改用无数字通用概述「本次研究的多源核验结果如下。」；无数字问题仍走「围绕「{问题}」开展…」原口径；空问题同通用概述。兜底 inferred 块与 §5.5 审计同口径，终稿不再可能出现零引用数字 conclusion。
2. `citation_audit.numeric_claim_binding_rate` 由硬编码 1.0 改为**遍历终稿 claim 块回算**（含数字断言块中带引用者占比，无数字论断块时为 1.0），恒等式变为可自检；`dropped_numeric_blocks`/`forced_inferred_blocks` 计数语义不变。
3. 回归测试：新增「数字问题 × 零 conclusion 存活」用例（断言块文本无 `2024`/`万辆`/问题原文、`has_numeric_assertion=False`、dropped=1、forced_inferred=1、回算绑定率 1.0）与空问题用例；全量 **636 passed**（+2），ruff 全净，mypy 54 条零新增。

前端复测建议：用交接 §9 同形态问题（含年份+万辆+百分比）重跑，预期该 run block-01 为通用概述（不含问题原文与数字）、`citation_audit.dropped_numeric_blocks=1`、`numeric_claim_binding_rate=1.0`；顺带补抽 §5 A3 真链 dispute。修复提交见 dev 最新 `fix(backend): ...` 提交。

## 10. 前端回归结论与证据（2026-09-14）

### 10.1 前端改动清单（§6 三组跟随项，零引擎改动）

| 文件 | 改动 |
| --- | --- |
| `frontend/src/services/mock/fixtures/demo_full.ts` | 新增 `BLOCK_CITATION_IDS` 矩阵为被引证据唯一事实源；`CITATION_MARKERS` 改为材料池顺序∩被引集合占号（demo 12 条恰全被引，输出仍 [1]~[12]）；`buildDemoCitations` 过滤未引用证据；新增 `buildDemoOutline`（sec-overview/sec-findings 恒定、sec-disputes 按需、sec-limitations 恒在），固定标题改「调研概述/核心发现/分歧与不确定性/局限与建议」 |
| `frontend/src/services/api/types.ts` | 仅注释清退：ReportBlock.id/conflict_id、StructuredReportResponse 超集/template_id·content_md 的「M2-7 冻结前/后、待联调确认」过期措辞；类型形态零变化 |
| `frontend/src/services/api/reports.ts`、`frontend/src/composables/useReportBlocks.ts`、`frontend/src/views/report/ReportView.vue` | 仅注释清退：「真链当前仅回 markdown」「冻结后删 markdown 分支」「mock 超集」类措辞改为冻结后实际口径（终稿超集→blocks；draft/历史/blocks 空→markdown）；运行代码零改动 |
| `docs/design/AI研究者助手-前端详细设计.md` | §7.4、§9.2 事件表、§11.4：blocks 切换口径由 `report.finished` 改为 `run.finished(status=succeeded)` 驱动（422 轮询），注明 mock 帧仅模拟 |
| `docs/design/AI研究者助手-前端M2任务分解与方案设计.md` | WP-16 双轨切换条目同步为 run.finished(succeeded) 驱动口径 |

### 10.2 真链样本（standard，dev@example.com，2026-09-14）

| run_id | 问题主题 | 耗时 | blocks（con/evi/dis/lim） | 信源/证据 | 冲突 | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 01M2G1N5YBTV2G21MFGKA2YPST | 新能源汽车销量/增长率（数字） | 87s | 12（1/8/0/3） | 14/24 | 1 medium resolved | **P1-1 命中**；余全过 |
| 01M2G2641DXT8VVSDQ0HM2K8XW | 出生人口机构口径（数字） | 60s | 13（4/6/0/3） | 12/19 | 0 | 全过（A2 浏览器取证 run，25 角标） |
| 01M2G2GAQ4YTE1J30GJDVNRMZP | 每日饮水量毫升之争 | 69s | 21（6/11/0/4） | 16/20 | 1 medium resolved | 全过（数字结论 6/6 带引用） |
| 01M2G2P301503QXMP316K5Q1BJ | 生酮饮食 RCT 减重公斤数 | 66s | 11（1/8/0/2） | 10/21 | 1 low resolved | 全过 |
| 01M2G53HDAR2Z7X3WA63K85CWJ | 饮酒安全量 Lancet 对立研究 | 42s | 13（2/8/0/3） | 14/22 | 0 | 全过 |

- 契约静态核对：`types.ts` 与 openapi-m2-7 逐字段一致（ReportBlock/ReportCitation 三字段/ReportCitationItem 10 字段/枚举/可选性差异 0）；实时服务 31 路径中 6 个未导出占位路由（users/teams/knowledge/connectors/templates/audit），冻结 25 路径全部在线。
- WS 全量帧采集（run#1/#2/#3）：`run.finished.payload.status=succeeded` 各 1 帧，全程 0 个 `report.finished`，与 §4 一致。
- 错误口径：不存在 run → 报告/citations 404；报告未生成 → 422 `{code:"validation_error",message:"报告尚未生成"}`（run 创建即测命中）；历史 draft 30+ run：200 + outline=[]/blocks=[]/citations 空列表；cancelled 无报告：422。
- 浏览器：真链 run#2 全交互 PASS（blocks 轨/目录/三档 confidence 色值/hover Popover/SourcePanel/键盘/外链/只看分歧灰化 opacity 0.4/LimitationSummary/三请求 200/控制台 0 报错）；mock demo_full PASS（4 语义目录、14 块、dispute 内联只读面板联动、[1]~[12]、无视觉回归）。
- 门禁：`npm run typecheck`（vue-tsc）、`npm run lint`（--max-warnings 0）、`npm run build` 全绿。
- 覆盖缺口（诚实标注）：①dispute 块真链零命中（5 run 的 3 个冲突全被权威规则自动 resolved，无 awaiting_human/both 保留），以 mock 组件实测 + 后端确定性单测/真库 m27 为据，修复复测轮补抽；②M2-5 剔除证据链本轮无窗口可重抽，以白名单装配逻辑 + M2-5 归档回归为据。
- 环境观察（非本包缺陷）：`POST /api/v1/telemetry/batch` 在 openapi-m2-5/m2-7 与实时服务均不存在，前端埋点静默失败仅 console warn（M2-3 以来既有口径）；浏览器代理注入脚本导致的噪声不计。
- 联调前置已执行：`alembic upgrade head` 0004→0005（report citations blocks）；抽验脚本留存前端本机 gitignored `tmp/m27/`（m27-regression.mjs / verify-draft.mjs / mock-drive.mjs 及 summary/frames/report 落盘）。
