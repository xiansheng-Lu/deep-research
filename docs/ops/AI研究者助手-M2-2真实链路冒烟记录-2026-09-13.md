# M2-2 真实链路冒烟记录（2026-09-13）

- 对应任务：M2-2 Task 11 TR-11.2
- 脚本：`backend/scripts/smoke_m22.py`（独立 schema `m22_smoke`，可重复执行）
- 环境：WSL2 Docker dr_postgres（deep_research 库）、AsyncPostgresSaver、DeepSeek deepseek-v4-flash（温度 0）
- 检索：固定高对立样本（离线）。说明：真实博查检索不保证同一时点同时返回对立结论，难以稳定构造 high 冲突，按任务书「测试库种子 run + 真实 API 调用」方式补证；LLM 判定、落库、鉴权、REST、检查点、恢复均为真实链路。

## 复跑方式

```bash
cd backend
TEST_DATABASE_URL=postgresql+asyncpg://deep_research:deep_research@localhost:5432/deep_research \
  uv run python scripts/smoke_m22.py
```

退出码 0 表示全部断言通过。

## 本次结果（退出码 0）

```text
[smoke] 首次执行：真实 DeepSeek + 真实 PostgresSaver
[smoke] run 已挂起：status=paused stage=critique
[smoke] GET 列表 200：1 条冲突
[smoke]   - 01M2BMZN7YWZDFH1MHCEY0XMQF type=methodological severity=high status=awaiting_human claim=2026年光伏新增装机规模
[smoke] GET 详情 200：evidence_a.title=国家统计局发布光伏新增装机规模数据；evidence_b.title=行业协会披露光伏新增装机规模口径
[smoke] 无 token 列表 401 OK
[smoke] POST 裁决 1/1 200：{'conflict_id': '01M2BMZN7YWZDFH1MHCEY0XMQF', 'status': 'resolved', 'verdict_id': '01M2BMZNSK5CFKCA3CT71GW14K'}
[smoke] 自动恢复完成：status=succeeded stage=report
```

报告「冲突与不确定性」段实际产出：

```markdown
## 冲突与不确定性

### 分歧与局限

以下分歧经裁决后仍作为不确定性保留，结论段不将任一方口径写为定论：

- **议题：2026年光伏新增装机规模**（口径/方法冲突，严重度：高）
  - 口径 A：国家统计局发布光伏新增装机规模数据
    - 来源：[国家统计局发布光伏新增装机规模数据](https://stats.gov.cn/pv-smoke)
  - 口径 B：行业协会披露光伏新增装机规模口径
    - 来源：[行业协会披露光伏新增装机规模口径](https://assoc.org.cn/pv-smoke)
  - 裁决意见：双方观点并存
  - 裁决理由：双方统计口径不同，观点并存
  - 局限备注：引用结论时必须注明口径差异
```

## 覆盖断言

1. 真实 LLM 检出 high 冲突后 run=paused/current_stage=critique；
2. 列表 200，字段含真实 type/severity/status/claim；
3. 详情 200 且 evidence_a/evidence_b 八项齐备；
4. 无 token 401；
5. both 裁决 200 返回三字段；
6. 后台自动恢复至 succeeded/report；
7. 报告含「分歧与局限」「双方观点并存」、双方来源链接、裁决理由与 additional_note。

真实检测质量的离线对照（20 条评估集，语义召回 66.67% vs 启发式 50%）另见 `backend/tests/eval/conflict_result.json` 与阶段技术方案 §8。
